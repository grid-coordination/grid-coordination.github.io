_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

# Source materials (local only)
_source/
.DS_Store

# Design handoff archives: the extracted files are what we track
design/*.zip

# Interrupted rebuilds must not leave a shadow copy of a deck next to the real one
.pv.md
